# Dask scheduler address (tcp://host:port)
# Only required when running multi-node experiments in examples with Dask.
# The topolink package is zero-config and does NOT require this setting.
DASK_CLIENT=tcp://127.0.0.1:8786